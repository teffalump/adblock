#/etc/config/dhcp

# Directly edit the file like following
config dnsmasq
    option ....
    option ...
    etc....
    list addnhosts '/etc/block.hosts' #ADD THIS LINE

# OR, execute this command once
uci add_list dhcp.@dnsmasq[0].addnhosts=/etc/block.hosts && uci commit